class InitializationResponse {
  String merchantId;
  String logoUrl;
  num amount;
  num actualAmout;
  num payInterswitch;

  InitializationResponse({this.merchantId, this.logoUrl, this.amount, this.actualAmout, this.payInterswitch});
}
